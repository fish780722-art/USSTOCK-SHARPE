!pip install ffn
import pandas as pd
import yfinance as yf
import ffn
from datetime import datetime
import numpy as np

# === 參數設定 ===
start_date = '2016-06-01'   # 過去十年的起始日
end_date = '2026-05-31'
rf_annual = 0.0368  # 無風險利率（年化），可改成美國3個月國庫券利率

# === 讀取 S&P500 公司清單 ===
sp500_companies = pd.read_csv('sp500_companies.csv')
sp500_companies['Date added'] = pd.to_datetime(sp500_companies['Date added'], errors='coerce')
sp500_companies = sp500_companies.dropna(subset=['Date added'])

# 篩選在 2015/9/1 前加入的公司
filtered_companies = sp500_companies[sp500_companies['Date added'] < datetime(2015, 9, 1)]
symbols = filtered_companies['Symbol'].tolist()

# === 分批下載資料，避免 yfinance timeout ===
batch_size = 50
dfs = []
for i in range(0, len(symbols), batch_size):
    batch = symbols[i:i+batch_size]
    df_batch = yf.download(batch, start=start_date, end=end_date, auto_adjust=True)['Close']
    dfs.append(df_batch)
data = pd.concat(dfs, axis=1)

# === 資料清理 ===
data = data.replace(0, np.nan).dropna(axis=1, how='any')  # 刪掉有0價的股票
data = data.dropna(how='any')  # 刪掉有缺值的日期

# === 轉成月度資料 ===
monthly_prices = data.resample('ME').last()
monthly_returns = monthly_prices.pct_change().dropna()

# === 無風險利率（月度） ===
rf_monthly = (1 + rf_annual)**(1/12) - 1

# === 計算最大化夏普比率的權重（基於月度資料） ===
weights = ffn.core.calc_mean_var_weights(monthly_returns).sort_values(ascending=False)
weights = weights[weights > 0]  # 剔除權重為0的標的

# === 計算績效（用月度資料） ===
weighted_returns = (monthly_returns[weights.index] * weights).sum(axis=1)

# 年化報酬率（CAGR）
cagr = (1 + weighted_returns.mean())**12 - 1
# 年化波動度
vol = weighted_returns.std() * np.sqrt(12)
# 夏普比率（扣除無風險利率）
sharpe = (weighted_returns.mean() - rf_monthly) / weighted_returns.std() * np.sqrt(12)

# === 輸出配置比率 ===
results = pd.DataFrame({
    'Symbol': weights.index,
    'Weight (%)': (weights * 100).round(2)
}).reset_index(drop=True)

print("=== 最佳夏普比率投資組合配置（基於月度資料） ===")
print(results)
print(f"\n年化報酬率 (CAGR): {cagr:.2%}")
print(f"年化波動度: {vol:.2%}")
print(f"夏普比率: {sharpe:.2f}")

# === 儲存到 CSV ===
results.to_csv('optimized_portfolio.csv', index=False)
